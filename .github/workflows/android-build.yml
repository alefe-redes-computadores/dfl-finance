name: Compilar APK DFL

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar Repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Configurar Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Instalar Dependências
        run: npm install

      - name: Build do Next.js
        run: npm run build

      - name: Sincronizar e Preparar Android
        run: |
          rm -rf android
          npx cap add android
          npx cap sync android
          
          # 🔥 MODO FORÇA BRUTA: Copiar a sua logo pro lugar exato que ele exige
          mkdir -p assets
          cp public/icon-512*.png assets/icon.png
          cp public/icon-512*.png assets/splash.png
          
          # Agora sim, forçar a geração por cima do projeto Android criado
          npx @capacitor/assets generate --android

      - name: Compilar APK (Gradle)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew clean assembleDebug

      - name: Disponibilizar APK
        uses: actions/upload-artifact@v4
        with:
          name: DFL-Finance-App
          path: android/app/build/outputs/apk/debug/app-debug.apk
